version: 1
backend:
  phases:
    build:
      commands:
        - npm install --legacy-peer-deps
frontend:
  phases:
    preBuild:
      commands:
        - npm install --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .amplify/artifacts/cdk.out
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*